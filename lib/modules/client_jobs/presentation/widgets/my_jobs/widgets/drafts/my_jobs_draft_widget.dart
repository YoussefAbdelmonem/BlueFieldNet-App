import 'package:flutter/material.dart';

class MyJobsDraftWidget extends StatelessWidget {
  const MyJobsDraftWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    body:SafeArea(child:Column(
     children: [],
    ))

    );
  }
}
