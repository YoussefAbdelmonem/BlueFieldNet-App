import 'package:flutter/material.dart';

class MyJobsPendingWidget extends StatelessWidget {
  const MyJobsPendingWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    body:SafeArea(child:Column(
     children: [],
    ))

    );
  }
}
