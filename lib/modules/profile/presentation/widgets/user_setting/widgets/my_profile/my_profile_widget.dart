import 'package:flutter/material.dart';

class MyProfileUserSettingsWidget extends StatelessWidget {
  const MyProfileUserSettingsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("MyProfileUserSettingsWidget"),
      ),
    );
  }
}
