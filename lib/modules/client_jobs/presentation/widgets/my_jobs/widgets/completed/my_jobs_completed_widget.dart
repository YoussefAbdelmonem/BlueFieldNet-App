import 'package:flutter/material.dart';

class MyJobsCompletedWidget extends StatelessWidget {
  const MyJobsCompletedWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    body:SafeArea(child:Column(
     children: [],
    ))

    );
  }
}
